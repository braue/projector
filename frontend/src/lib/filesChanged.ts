/** Window event a tool fires after changing a project's files behind the
 *  sidebar's back (e.g. the DAC SIM Converter placing generated entries) —
 *  the app reloads the tree on it. */
export const FILES_CHANGED_EVENT = 'projector:files-changed'
